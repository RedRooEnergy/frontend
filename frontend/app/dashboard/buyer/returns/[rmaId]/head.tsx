import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Return Detail | RedRooEnergy",
  description: "Return request detail",
  robots: { index: false, follow: false },
};

export default function Head() {
  return null;
}
