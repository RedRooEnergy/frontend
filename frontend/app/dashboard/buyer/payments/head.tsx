import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Buyer Payments & Billing | RedRooEnergy",
  description: "Buyer payments and billing dashboard.",
  robots: { index: false, follow: false },
  alternates: { canonical: "/dashboard/buyer/payments" },
};

export default function Head() {
  return null;
}
