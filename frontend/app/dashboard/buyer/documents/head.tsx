import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Buyer Documents | RedRooEnergy",
  description: "Buyer document vault.",
  robots: { index: false, follow: false },
  alternates: { canonical: "/dashboard/buyer/documents" },
};

export default function Head() {
  return null;
}
