import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Support Ticket | RedRooEnergy",
  description: "Buyer support ticket detail",
  robots: { index: false, follow: false },
};

export default function Head() {
  return null;
}
