import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Service Partner Messages | RedRooEnergy",
  description: "Service partner messages and notifications dashboard.",
  robots: { index: false, follow: false },
  alternates: { canonical: "/dashboard/service-partner/messages" },
};

export default function Head() {
  return null;
}
