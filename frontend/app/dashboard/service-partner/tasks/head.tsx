import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Service Partner Tasks | RedRooEnergy",
  description: "Service partner task management dashboard.",
  robots: { index: false, follow: false },
  alternates: { canonical: "/dashboard/service-partner/tasks" },
};

export default function Head() {
  return null;
}
